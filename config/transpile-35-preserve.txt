# Paths (git pathspecs/globs) that are specific to the v3.5 line and must survive the
# transpile overlay. After the transpiled master tree is rsync'd over src/includes/tests,
# these are restored from the branch HEAD. One pattern per line; blank lines and # ignored.

# Version-specific snapshot fixtures generated for the 3.5 line (the 4.x tree has its own).
tests/*__3.5__*

# Runtime shim defining PHP 8.0 tokenizer constants used by Utils\Packer on PHP < 8.0.
src/tokens-polyfill.php

# v3.5-specific environment (snapshot version tag, service ports) the overlay must not clobber.
tests/.env
