
### Class: \Codeception\Module\WPFilesystem

> Class WPFilesystem WordPress specific filesystem operations.

<table style="width: 100%;">
        <thead>
        <tr>
            <th>Method</th>
            <th>Example</th>
        </tr>
        </thead>
<tr><td><strong>amInMuPluginPath(</strong><em>string</em> <strong>$path</strong>)</strong> : <em>void</em><br /><br /><em>Sets the current working directory to a directory in a mu-plugin. ``` php <?php $I->amInMuPluginPath('mu-plugin'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$path</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>amInPluginPath(</strong><em>string</em> <strong>$path</strong>)</strong> : <em>void</em><br /><br /><em>Sets the current working directory to a directory in a plugin. ``` php <?php $I->amInPluginPath('my-plugin'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$path</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>amInThemePath(</strong><em>string</em> <strong>$path</strong>)</strong> : <em>void</em><br /><br /><em>Sets the current working directory to a directory in a theme. ``` php <?php $I->amInThemePath('my-theme'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$path</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>amInUploadsPath(</strong><em>string</em> <strong>$path=null</strong>)</strong> : <em>void</em><br /><br /><em>Enters the uploads folder in the local filesystem.</em><p><strong>Parameters:</strong><ul>string <strong>$path</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>cleanMuPluginDir(</strong><em>string</em> <strong>$dir</strong>)</strong> : <em>void</em><br /><br /><em>Cleans a directory in a mu-plugin directory. ``` php <?php $I->cleanMuPluginDir('mu-plugin1/foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$dir</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>cleanPluginDir(</strong><em>string</em> <strong>$dir</strong>)</strong> : <em>void</em><br /><br /><em>Cleans a directory in a plugin directory. ``` php <?php $I->cleanPluginDir('plugin1/foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$dir</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>cleanThemeDir(</strong><em>string</em> <strong>$dir</strong>)</strong> : <em>void</em><br /><br /><em>Clears a directory in a theme directory. ``` php <?php $I->cleanThemeDir('my-theme/foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$dir</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>cleanUploadsDir(</strong><em>string</em> <strong>$dir=null</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>void</em><br /><br /><em>Clears a directory in the uploads folder. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. ``` php <?php $I->cleanUploadsDir('some/folder'); $I->cleanUploadsDir('some/folder', 'today'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$dir</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>copyDirToMuPlugin(</strong><em>string</em> <strong>$src</strong>, <em>string</em> <strong>$pluginDst</strong>)</strong> : <em>void</em><br /><br /><em>Copies a directory to a directory in a mu-plugin. ``` php <?php $I->copyDirToMuPlugin(codecept_data_dir('foo'), 'mu-plugin/foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$src</strong>: 
string <strong>$pluginDst</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>copyDirToPlugin(</strong><em>string</em> <strong>$src</strong>, <em>string</em> <strong>$pluginDst</strong>)</strong> : <em>void</em><br /><br /><em>Copies a directory to a directory in a plugin. ``` php <?php $I->copyDirToPlugin(codecept_data_dir('foo'), 'plugin/foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$src</strong>: 
string <strong>$pluginDst</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>copyDirToTheme(</strong><em>string</em> <strong>$src</strong>, <em>string</em> <strong>$themeDst</strong>)</strong> : <em>void</em><br /><br /><em>Copies a directory in a theme directory. ``` php <?php $I->copyDirToTheme(codecept_data_dir('foo'), 'my-theme'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$src</strong>: 
string <strong>$themeDst</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>copyDirToUploads(</strong><em>string</em> <strong>$src</strong>, <em>string</em> <strong>$dst</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>void</em><br /><br /><em>Copies a directory to the uploads folder. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. ``` php <?php $I->copyDirToUploads(codecept_data_dir('foo'), 'uploadsFoo'); $I->copyDirToUploads(codecept_data_dir('foo'), 'uploadsFoo', 'today'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$src</strong>: 
string <strong>$dst</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>deleteMuPluginFile(</strong><em>string</em> <strong>$file</strong>)</strong> : <em>void</em><br /><br /><em>Deletes a file in a mu-plugin directory. ``` php <?php $I->deleteMuPluginFile('mu-plugin1/some-file.txt'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>deletePluginFile(</strong><em>string</em> <strong>$file</strong>)</strong> : <em>void</em><br /><br /><em>Deletes a file in a plugin directory. ``` php <?php $I->deletePluginFile('plugin1/some-file.txt'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>deleteThemeFile(</strong><em>string</em> <strong>$file</strong>)</strong> : <em>void</em><br /><br /><em>Deletes a file in a theme directory. ``` php <?php $I->deleteThemeFile('my-theme/some-file.txt'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>deleteUploadedDir(</strong><em>string</em> <strong>$dir</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>void</em><br /><br /><em>Deletes a dir in the uploads folder. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. ``` php <?php $I->deleteUploadedDir('folder'); $I->deleteUploadedDir('folder', 'today'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$dir</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>deleteUploadedFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>void</em><br /><br /><em>Deletes a file in the uploads folder. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. ``` php <?php $I->deleteUploadedFile('some-file.txt'); $I->deleteUploadedFile('some-file.txt', 'today'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>dontSeeInMuPluginFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$contents</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file in a mu-plugin directory does not contain a string. ``` php <?php $I->dontSeeInMuPluginFile('mu-plugin1/some-file.txt', 'foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$contents</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>dontSeeInPluginFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$contents</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file in a plugin directory does not contain a string. ``` php <?php $I->dontSeeInPluginFile('plugin1/some-file.txt', 'foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$contents</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>dontSeeInThemeFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$contents</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file in a theme directory does not contain a string. ``` php <?php $I->dontSeeInThemeFile('my-theme/some-file.txt', 'foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$contents</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>dontSeeInUploadedFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$contents</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file in the uploads folder does contain a string. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. ``` php <?php $I->dontSeeInUploadedFile('some-file.txt', 'foo'); $I->dontSeeInUploadedFile('some-file.txt','foo', 'today'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$contents</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>dontSeeMuPluginFileFound(</strong><em>string</em> <strong>$file</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file is not found in a mu-plugin directory. ``` php <?php $I->dontSeeMuPluginFileFound('mu-plugin1/some-file.txt'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>dontSeePluginFileFound(</strong><em>string</em> <strong>$file</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file is not found in a plugin directory. ``` php <?php $I->dontSeePluginFileFound('plugin1/some-file.txt'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>dontSeeThemeFileFound(</strong><em>string</em> <strong>$file</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file is not found in a theme directory. ``` php <?php $I->dontSeeThemeFileFound('my-theme/some-file.txt'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>dontSeeUploadedFileFound(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>void</em><br /><br /><em>Checks thata a file does not exist in the uploads folder. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. ``` php <?php $I->dontSeeUploadedFileFound('some-file.txt'); $I->dontSeeUploadedFileFound('some-file.txt','today'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>getUploadsPath(</strong><em>string</em> <strong>$file=`''`</strong>, <em>null</em> <strong>$date=null</strong>)</strong> : <em>string The absolute path to an uploaded file.</em><br /><br /><em>Returns the path to the specified uploads file of folder. Not providing a value for `$file` and `$date` will return the uploads folder path. a UNIX timestamp or a string supported by the `strtotime` function; defaults to `now`.</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: The file path, relative to the uploads folder.
null <strong>$date</strong>: The date that should be used to build the uploads sub-folders in the year/month format;</ul></p></td><td></td></tr>
<tr><td><strong>getWpRootFolder()</strong> : <em>string</em><br /><br /><em>Returns the absolute path to WordPress root folder without trailing slash.</em></td><td></td></tr>
<tr><td><strong>haveMuPlugin(</strong><em>string</em> <strong>$filename</strong>, <em>string</em> <strong>$code</strong>)</strong> : <em>bool</em><br /><br /><em>Creates a mu-plugin file, including plugin header, in the mu-plugins folder. The code should not contain the opening '<?php' tag. ``` php <?php $code = 'echo "Hello world!"'; $I->haveMuPlugin('foo-mu-plugin.php', $code); ?> ``` plugin file to create. php tag.</em><p><strong>Parameters:</strong><ul>string <strong>$filename</strong>: The path, relative to the plugins folder, of the
string <strong>$code</strong>: The content of the plugin file without the opening</ul></p></td><td></td></tr>
<tr><td><strong>havePlugin(</strong><em>string</em> <strong>$path</strong>, <em>string</em> <strong>$code</strong>)</strong> : <em>bool</em><br /><br /><em>Creates a plugin file, including plugin header, in the plugins folder. The plugin is just created and not activated; the code should not contain the opening '<?php' tag. ``` php <?php $code = 'echo "Hello world!"'; $I->havePlugin('foo/plugin.php', $code); ?> ``` plugin file to create. php tag.</em><p><strong>Parameters:</strong><ul>string <strong>$path</strong>: The path, relative to the plugins folder, of the
string <strong>$code</strong>: The content of the plugin file without the opening</ul></p></td><td></td></tr>
<tr><td><strong>haveTheme(</strong><em>string</em> <strong>$folder</strong>, <em>string</em> <strong>$indexFileCode</strong>, <em>string</em> <strong>$functionsFileCode=null</strong>)</strong> : <em>bool</em><br /><br /><em>Creates a theme file structure, including theme style file and index, in the themes folder. The theme is just created and not activated; the code should not contain the opening '<?php' tag. ``` php <?php $code = 'sayHi();'; $functionsCode  = 'function sayHi(){echo "Hello world";};'; $I->haveTheme('foo', $indexCode, $functionsCode); ?> ``` folder, of the plugin directory to create. without the opening php tag. file without the opening php tag.</em><p><strong>Parameters:</strong><ul>string <strong>$folder</strong>: The path, relative to the themes
string <strong>$indexFileCode</strong>: The content of the theme index.php file
string <strong>$functionsFileCode</strong>: The content of the theme functions.php</ul></p></td><td></td></tr>
<tr><td><strong>openUploadedFile(</strong><em>string</em> <strong>$filename</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>void</em><br /><br /><em>Opens a file in the the uploads folder. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. ``` php <?php $I->openUploadedFile('some-file.txt'); $I->openUploadedFile('some-file.txt', 'time'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$filename</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>seeInMuPluginFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$contents</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file in a mu-plugin directory contains a string. ``` php <?php $I->seeInMuPluginFile('mu-plugin1/some-file.txt', 'foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$contents</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>seeInPluginFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$contents</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file in a plugin directory contains a string. ``` php <?php $I->seeInPluginFile('plugin1/some-file.txt', 'foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$contents</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>seeInThemeFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$contents</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file in a theme directory contains a string. ``` php <?php $I->seeInThemeFile('my-theme/some-file.txt', 'foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$contents</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>seeInUploadedFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$contents</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file in the uploads folder contains a string. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. ``` php <?php $I->seeInUploadedFile('some-file.txt', 'foo'); $I->seeInUploadedFile('some-file.txt','foo', 'today'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$contents</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>seeMuPluginFileFound(</strong><em>string</em> <strong>$file</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file is found in a mu-plugin directory. ``` php <?php $I->seeMuPluginFileFound('mu-plugin1/some-file.txt'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>seePluginFileFound(</strong><em>string</em> <strong>$file</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file is found in a plugin directory. ``` php <?php $I->seePluginFileFound('plugin1/some-file.txt'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>seeThemeFileFound(</strong><em>string</em> <strong>$file</strong>)</strong> : <em>void</em><br /><br /><em>Checks that a file is found in a theme directory. ``` php <?php $I->seeThemeFileFound('my-theme/some-file.txt'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>seeUploadedFileFound(</strong><em>string</em> <strong>$filename</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>void</em><br /><br /><em>Checks if file exists in the uploads folder. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. Opens a file when it's exists ``` php <?php $I->seeUploadedFileFound('some-file.txt'); $I->seeUploadedFileFound('some-file.txt','today'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$filename</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>writeToMuPluginFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$data</strong>)</strong> : <em>void</em><br /><br /><em>Writes a file in a mu-plugin directory. ``` php <?php $I->writeToMuPluginFile('mu-plugin1/some-file.txt', 'foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$data</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>writeToPluginFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$data</strong>)</strong> : <em>void</em><br /><br /><em>Writes a file in a plugin directory. ``` php <?php $I->writeToPluginFile('plugin1/some-file.txt', 'foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$data</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>writeToThemeFile(</strong><em>string</em> <strong>$file</strong>, <em>string</em> <strong>$data</strong>)</strong> : <em>void</em><br /><br /><em>Writes a string to a file in a theme directory. ``` php <?php $I->writeToThemeFile('my-theme/some-file.txt', 'foo'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$file</strong>: 
string <strong>$data</strong>: </ul></p></td><td></td></tr>
<tr><td><strong>writeToUploadedFile(</strong><em>string</em> <strong>$filename</strong>, <em>string</em> <strong>$data</strong>, <em>string</em> <strong>$date=null</strong>)</strong> : <em>string The absolute path to the destination file.</em><br /><br /><em>Writes a string to a file in the the uploads folder. The date argument can be a string compatible with `strtotime` or a Unix timestamp that will be used to build the `Y/m` uploads subfolder path. ``` php <?php $I->writeToUploadedFile('some-file.txt', 'foo bar'); $I->writeToUploadedFile('some-file.txt', 'foo bar', 'today'); ?> ```</em><p><strong>Parameters:</strong><ul>string <strong>$filename</strong>: 
string <strong>$data</strong>: 
string <strong>$date</strong>: </ul></p></td><td></td></tr></table>

*This class extends \Codeception\Module\Filesystem*

